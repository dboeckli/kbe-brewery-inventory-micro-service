server:
  port: 8082

spring:
  application:
    name: kbe-brewery-inventory-micro-service
    
  docker:
    compose:
      enabled: true
      stop:
        command: down
      
  jackson:
    serialization:
      write-dates-as-timestamps: false
      write-date-timestamps-as-nanoseconds: true
      
  zipkin:
    enabled: false
    
  cloud:
    discovery:
      enabled: false
      
  artemis:
    user: artemis
    password: simetraehcapa
    broker-url: tcp://localhost:61616
    
  security:
    user:
      name: good
      password: beer
      
  datasource:
    url: jdbc:mysql://localhost:3306/beerservice?useUnicode=true&character_set_server=utf8mb4&characterEncoding=UTF-8&serverTimezone=UTC
    #url: jdbc:mysql://localhost:3306/beerservice?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: dbpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        # Show SQL  
        show_sql: true
        # Format SQL
        format_sql: true
        
  data:
    jpa:
      repositories:
        bootstrap-mode: default

management:
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always

logging:
  level:
    org.zalando.logbook: TRACE

logbook:
  filter:
    enabled: true
  format:
    style: http
  obfuscate:
    headers:
      - Authorization
      - X-Secret
    parameters:
      - access_token
      - password
  predicate:
    exclude:
      - path: /actuator/health
